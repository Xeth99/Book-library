<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>zXyk Books Store</title>

    <style>
        body {
          font-family: Arial, sans-serif;
          margin: 20px;
        }
    
        h1 {
          color: #333;
        }
    
        #bookList {
          list-style: none;
          padding: 0;
        }
    
        .book {
          border: 1px solid #ccc;
          margin: 10px;
          padding: 10px;
          border-radius: 5px;
        }
      </style>
</head>
<body>
    <h1>Book List</h1>

    const books = [
    {
      title: 'Book 1',
      description: 'Description of Book 1',
      genre: 'Fiction',
      datePublished: '2022-01-01',
      pageCount: 200,
      publisher: 'Publisher A',
    },
    {
      title: 'Book 2',
      description: 'Description of Book 2',
      genre: 'Non-Fiction',
      datePublished: '2022-02-01',
      pageCount: 300,
      publisher: 'Publisher B',
    },
  ];

  <!-- <ul id="bookList">
    <% books.forEach(book => { %>
      <li class="book">
        <h3><%= book.title %></h3>
        <p><strong>Description:</strong> <%= book.description %></p>
        <p><strong>Genre:</strong> <%= book.genre %></p>
        <p><strong>Published Date:</strong> <%= new Date(book.datePublished).toLocaleDateString() %></p>
        <p><strong>Page Count:</strong> <%= book.pageCount %></p>
        <p><strong>Publisher:</strong> <%= book.publisher %></p>
      </li>
    <% }); %>
  </ul> -->





  <% if (Array.isArray(books)) { %>
    <ul id="bookList">
      <% book.forEach(book => { %>
        <li class="book">
          <h3><%= book.title %></h3>
          <p><strong>Description:</strong> <%= book.description %></p>
          <p><strong>Genre:</strong> <%= book.genre %></p>
          <p><strong>Published Date:</strong> <%= new Date(book.datePublished).toLocaleDateString() %></p>
          <p><strong>Page Count:</strong> <%= book.pageCount %></p>
          <p><strong>Publisher:</strong> <%= book.publisher %></p>
        </li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>No books available.</p>
  <% } %>
  
</body>
</html>